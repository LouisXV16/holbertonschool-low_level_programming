#include "main.h"

/**
 * _abs - compute the absolute value of an integrer
 *@n number to be checked
 * Return: -n if n<0, n else
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
