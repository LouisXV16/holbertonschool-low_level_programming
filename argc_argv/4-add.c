#include <stdio.h>
#include <stdlib.h>

/**
*main - print prod of number of arg
*@argc: size of argv
*@argv: array of program
*Return: always 0
*/

int main(int argc, char *argv[])
{
	int numi;

	if (argc == 1)
		printf("0\n");
		return (0);

	if (argc <= 2)
	{
		printf("Error\n");
		return (1);
	}

	numi = atoi(argv[i]);

	printf("%d\n", numi + );

	return (0);
}

